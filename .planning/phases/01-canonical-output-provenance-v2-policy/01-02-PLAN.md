---
phase: 01
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/database.js
  - backend/videoProcessor.js
  - backend/modules/promptPolicy.js
  - backend/modules/describer.js
  - backend/routes.js
  - frontend/src/screens/PlayerScreenV2.js
  - backend/test_prompt_policy.js
  - backend/test_canonical_integration.js
autonomous: true
requirements:
  - POLICY-01
  - POLICY-02
  - POLICY-03
  - POLICY-04
---

<objective>
Integrate the canonical validator into every generation path, persistence/API projection, prompt loading, and player/TTS selection so only accepted policy-compliant events are emitted, stored as playable output, returned to the client, and synthesized.
</objective>

<context>
Plan 01 provides the pure contract. Current interactive and batch paths duplicate parsing and persist/emit before complete validation; prompt defaults drift from v2; stage2_describer is a second prompt loader; database/player use legacy fields. Integration must preserve SSE event names and legacy response fields while enforcing canonical status and TTS eligibility.
</context>

<tasks>

<task type="execute">
<name>Add additive canonical persistence, quarantine storage, and legacy projection</name>
<files>backend/database.js</files>
<read_first>
- backend/database.js:31-79 (additive migration conventions and scripts schema)
- backend/database.js:345-401 (getVideo/saveVideo legacy projection and transaction)
- backend/database.js:516-532 (chunk persistence)
- backend/modules/canonicalOutput.js (event fields and status/reason contract from Plan 01)
- .planning/PROJECT.md (SQLite compatibility constraint)
</read_first>
<action>
Extend `scripts` with additive columns `tag TEXT`, `provenance_json TEXT`, `validation_status TEXT NOT NULL DEFAULT 'accepted'`, `validation_reasons_json TEXT NOT NULL DEFAULT '[]'`, `tts_eligible INTEGER NOT NULL DEFAULT 1`, and `policy_version TEXT`. Add `script_quarantine(id TEXT PRIMARY KEY, videoId TEXT NOT NULL, raw_line TEXT, candidate_json TEXT NOT NULL, reason_code TEXT NOT NULL, reason_detail TEXT, createdAt DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(videoId) REFERENCES videos(videoId) ON DELETE CASCADE)` in `init()` using the existing startup migration convention. Implement `saveCanonicalScriptChunk({videoId, events})` to insert only accepted events in one transaction with `INSERT OR IGNORE`, implement `saveQuarantinedScriptEvents({videoId, candidates})` with bounded raw-line/reason fields, and update `getVideo(videoId)` to return legacy `verbosity` plus `tag`, parsed provenance, `validationStatus`, `validationReasons`, `ttsEligible`, and `policyVersion`. Keep `saveVideo` as a compatibility wrapper that accepts projected legacy events but routes through canonical columns; do not delete or rename existing columns.
</action>
<acceptance_criteria>
- Fresh initialization creates the new scripts columns and `script_quarantine`; an existing database gains them through additive migrations without dropping data.
- `saveCanonicalScriptChunk` persists only events with `validationStatus === 'accepted'`; rejected/quarantined candidates are absent from `scripts` and can be found in `script_quarantine` with a reason code.
- `getVideo(videoId).script` retains `id`, numeric `timestamp`, `text`, and legacy `verbosity`, and also returns `tag`, `validationStatus`, `ttsEligible`, `policyVersion`, and provenance.
- Duplicate accepted event IDs remain idempotent under repeated `saveCanonicalScriptChunk` calls.
- `node --test backend/test_canonical_integration.js` verifies migration, accepted-only persistence, quarantine persistence, and legacy projection using a disposable SQLite database.
</acceptance_criteria>
<verify>
Run `node --test backend/test_canonical_integration.js`; inspect the disposable schema with SQLite queries and confirm no production `backend/db/cache.db` is modified.
</verify>
<automated>node --test backend/test_canonical_integration.js</automated>
</task>

<task type="execute">
<name>Centralize v2 prompt loading and protect alternate prompt paths</name>
<files>backend/modules/promptPolicy.js, backend/videoProcessor.js, backend/modules/describer.js, backend/test_prompt_policy.js</files>
<read_first>
- backend/videoProcessor.js:53-82 and :730-740 (current prompt resolution/substitution)
- backend/modules/describer.js:1-47 (alternate stage2 prompt loader)
- backend/prompt_template_codex_v2.txt (required common policy text and placeholders)
- backend/prompts/stage2_describer.txt (legacy prompt content that must not bypass v2)
- backend/modules/canonicalOutput.js (POLICY_VERSION constant)
- .planning/PROJECT.md:48-49 (prompt/config drift decision)
</read_first>
<action>
Create CommonJS backend/modules/promptPolicy.js exporting `POLICY_VERSION = 'codex-v2'`, `DEFAULT_POLICY_PROMPT_FILE = 'prompt_template_codex_v2.txt'`, `loadPolicyPrompt({promptFile, replacements})`, and `assertV2PolicyPrompt(prompt)`. Make `loadPolicyPrompt` resolve relative files from `backend/`, default to v2, replace all required placeholders (`VIDEO_TITLE`, `AUDIO_CLASSIFICATION`, `AUDIO_LANGUAGE`, `DIALOGUE_TRACK`) without leaving `{{...}}`, and throw a named error when the prompt is missing required v2 policy markers or attempts to use an unsupported tag set. Update both interactive and batch `videoProcessor.js` paths to use it. Update `backend/modules/describer.js` to compose its stage-specific content with the same v2 policy baseline or fail closed if the legacy stage prompt cannot be safely composed; it must not independently load an unvalidated policy prompt. Add `policyVersion: 'codex-v2'` to generation context passed to canonical validation.
</action>
<acceptance_criteria>
- With `PROMPT_FILE` unset, `loadPolicyPrompt()` reads exactly `backend/prompt_template_codex_v2.txt`.
- The loaded prompt contains the v2 tag set `[v1]`, `[v2]`, `[v3]`, `[txt]`, `[trans]`, no unresolved required placeholders, and `POLICY_VERSION === 'codex-v2'` is returned with the prompt/context.
- Interactive, batch, and `describeSegment` no longer have an independent prompt-loading path that bypasses `assertV2PolicyPrompt`.
- A missing file, unresolved required placeholder, unsupported tag marker, or policy assertion failure throws before any Gemini call.
- `node --test backend/test_prompt_policy.js` exits 0 and includes a test for default-v2 resolution and alternate-path policy enforcement.
</acceptance_criteria>
<verify>
Run `node --test backend/test_prompt_policy.js`; use a temporary `PROMPT_FILE` fixture to verify safe failure for a legacy prompt without the v2 baseline.
</verify>
<automated>node --test backend/test_prompt_policy.js</automated>
</task>

<task type="execute">
<name>Route interactive and batch generation through accepted-only canonical publication</name>
<files>backend/videoProcessor.js, backend/test_canonical_integration.js</files>
<read_first>
- backend/videoProcessor.js:705-825 and :1134-1226 (interactive/batch generation and current early save/filter behavior)
- backend/modules/canonicalOutput.js (Plan 01 API)
- backend/database.js (Plan 02 persistence API)
- backend/routes.js:136-150 (legacy script response)
- .planning/phases/01-canonical-output-provenance-v2-policy/01-VALIDATION.md
</read_first>
<action>
Replace the two duplicated regex/filter implementations in `processVideo` and `processVideoBatch` with one shared `canonicalizeModelOutput(rawText, context)` call using `validateEvents`. Accumulate the model stream text until the response is complete, then parse and validate the complete candidate set before emitting any `script_chunk` event or calling canonical persistence. Call `db.saveCanonicalScriptChunk` only with accepted events, call `db.saveQuarantinedScriptEvents` for quarantined/rejected candidates with reason codes, and emit `script_chunk` using `toLegacyScriptEvent` only for accepted events. Preserve existing `start`, `status_update`, `script_chunk`, and `end` SSE names and sort accepted events by canonical timestamp. Ensure final `saveVideo`/status completion cannot mark output playable when validation failed or all candidates were quarantined. Add integration fixtures that feed the same raw model output through both interactive-style and batch-style helpers and assert identical canonical IDs, statuses, legacy projections, accepted persistence, and quarantine reasons.
</action>
<acceptance_criteria>
- No call to `sseHandler('script_chunk', ...)`, `saveVideoChunk`, or `saveCanonicalScriptChunk` occurs before complete canonical validation.
- Interactive and batch paths invoke the same exported/shared canonicalization helper and produce byte-equivalent accepted legacy projections for the same fixture.
- Invalid, unsafe, duplicate, and TTS-ineligible candidates are absent from `script_chunk` and `scripts`; their reason codes are in quarantine diagnostics.
- Accepted events remain sorted by numeric timestamp and preserve the original SSE event names.
- `node --test backend/test_canonical_integration.js` verifies no premature publication and parity between both paths with mocked model text only.
</acceptance_criteria>
<verify>
Run `node --test backend/test_canonical_integration.js`; grep `backend/videoProcessor.js` to confirm both generation paths reference `canonicalizeModelOutput` and no legacy regex parser remains in either path.
</verify>
<automated>node --test backend/test_canonical_integration.js</automated>
</task>

<task type="execute">
<name>Enforce canonical eligibility in API and Player V2 TTS selection</name>
<files>backend/routes.js, frontend/src/screens/PlayerScreenV2.js, backend/test_canonical_integration.js</files>
<read_first>
- frontend/src/screens/PlayerScreenV2.js:466-493 and :520-570 (legacy filtering, playableScript, and TTS request)
- backend/routes.js:136-150 and the `/api/tts` route (script and TTS API contracts)
- backend/database.js:getVideo (canonical-to-legacy projection)
- backend/modules/canonicalOutput.js (status and ttsEligible semantics)
- .planning/REQUIREMENTS.md:12-13 (POLICY-03/POLICY-04)
</read_first>
<action>
Update `getVideo` response handling and `PlayerScreenV2` filtering so `filteredScript`, `playableScript`, and `playDescription` ignore any line where `validationStatus !== 'accepted'` or `ttsEligible === false`, while preserving existing verbosity/verbosity controls for accepted lines. Include `videoId` and `eventId` in TTS requests. Update `/api/tts` to require a canonical accepted event lookup by `videoId` and `eventId` before synthesizing; reject missing, quarantined, rejected, or `ttsEligible=false` events with HTTP 422 and do not synthesize raw arbitrary text. Keep the existing cache behavior keyed by accepted event identity and synthesis parameters. Add pure/integration assertions for a Korean duplicate, quarantined OCR, accepted foreign translation, and accepted visual event reaching/not reaching the TTS boundary.
</action>
<acceptance_criteria>
- Player selection excludes `validationStatus: 'quarantined'`, `validationStatus: 'rejected'`, and `ttsEligible: false` before `playDescription` is called.
- Accepted `text`, `translation`, and `v1`–`v3` events retain existing verbosity and user settings behavior.
- `/api/tts` returns 422 and makes zero provider calls for unknown event IDs, non-accepted events, and `ttsEligible=false`; it synthesizes only an accepted event fetched by `videoId`/`eventId`.
- A Korean original duplicate cannot reach the TTS request even if its legacy verbosity is `translation`.
- `node --test backend/test_canonical_integration.js` exits 0 for API/player eligibility fixtures without invoking Google TTS.
</acceptance_criteria>
<verify>
Run `node --test backend/test_canonical_integration.js`; inspect the request fixture to confirm every TTS call carries `videoId` and accepted `eventId`, never free text alone.
</verify>
<automated>node --test backend/test_canonical_integration.js</automated>
</task>

</tasks>

<threat_model>
<threat id="T-04" severity="high">Prompt injection in title, captions, OCR, or visible signs could alter output format or policy. Mitigation: assert the common v2 prompt baseline before Gemini and treat all inserted fields as data; canonical parser accepts only allowlisted tags.</threat>
<threat id="T-05" severity="high">Unvalidated events could be emitted through SSE, persisted, or synthesized during retries/cache hits. Mitigation: buffer until canonical validation, persist accepted-only, expose statuses, and require accepted event ID lookup at /api/tts.</threat>
<threat id="T-06" severity="medium">Provenance JSON or quarantine data could leak full captions/prompts into logs/API. Mitigation: return bounded structured fields, keep quarantine internal, and never include raw prompt/evidence payloads in SSE or TTS responses.</threat>
</threat_model>

<verification>
- `node --test backend/test_canonical_output.js backend/test_audio_language_policy.js backend/test_prompt_policy.js backend/test_canonical_integration.js` exits 0.
- SQLite migration is additive and legacy `/api/script/:videoId` fields remain present.
- Both generation paths publish only accepted canonical events and use `POLICY_VERSION: 'codex-v2'`.
- Player and `/api/tts` cannot synthesize a rejected/quarantined/Korean-duplicate event.
- POLICY-01 through POLICY-04 are covered by the plan frontmatter and deterministic tests.
</verification>

<success_criteria>
- Every supported prompt path uses the common v2 policy baseline.
- Canonical provenance and validation status survive persistence and legacy serialization.
- No unsafe or original-dialogue-duplicate event reaches SSE playable output or TTS.
- Existing timed tags, timestamps, SSE event names, and player controls remain compatible for accepted events.
</success_criteria>
